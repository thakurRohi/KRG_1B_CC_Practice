#include <bits/stdc++.h>
using namespace std;

vector<int> getMaxParts(string packages) {
    
}

int main() {
    string packages;
    cin >> packages;
    
    vector<int> ans = getMaxParts(packages);
    for (int x : ans) cout << x << " ";
    
    return 0;
}